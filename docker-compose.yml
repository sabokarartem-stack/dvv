services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rails-api
    ports:
      - "3000:3000"
    environment:
      - RAILS_ENV=development
    restart: unless-stopped
